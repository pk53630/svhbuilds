# Base URL of the backend API. During local dev, Vite proxies /api to
# http://localhost:4000 automatically (see vite.config.js), so you can leave
# this blank for local development. Set it to your deployed API URL in
# production, e.g. https://api.yourdomain.com
VITE_API_URL=
